<?xml version="1.0" encoding="utf-8" ?>
<toolkit:Popup xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:local="clr-namespace:SlippiTV.Client"
             x:TypeArguments="system:String"
             x:Class="SlippiTV.Client.InputTextPopup"
             x:DataType="{x:Type local:InputTextPopup}"
             Padding="0"
             Margin="0"
             BackgroundColor="Transparent">
    
    <Border BackgroundColor="{AppThemeBinding Light=#DCDCDC, Dark=#333333}" StrokeShape="RoundRectangle 24" Margin="0" Padding="0" StrokeThickness="0" WidthRequest="360">
        <VerticalStackLayout Spacing="16" Padding="24,24,24,12">
            <VerticalStackLayout Spacing="8">
                <Label Text="{Binding Title}"
                   FontSize="20"
                   FontAttributes="Bold"
                   TextColor="{AppThemeBinding Light=#333333, Dark=#FAFAFA}"
                   VerticalTextAlignment="Center"
                   HorizontalTextAlignment="Center"
                   />
                <BoxView HeightRequest="1" Color="#333333"/>
            </VerticalStackLayout>

            <Border StrokeShape="RoundRectangle 12" StrokeThickness="0">
                <Entry x:Name="ConnectCodeEntry" Placeholder="{Binding PlaceholderText}" TextColor="Black" BackgroundColor="{AppThemeBinding Light=#f1f1f1, Dark=#444444}"/>
            </Border>

            <FlexLayout AlignContent="Center" VerticalOptions="Center" HorizontalOptions="Center">
                <HorizontalStackLayout FlexLayout.Shrink="1" Spacing="20">
                    <Button x:Name="SubmitButton" Text="Submit" Clicked="SubmitButton_Clicked"/>
                    <Button x:Name="CancelButton" Text="Cancel" Clicked="CancelButton_Clicked"/>
                </HorizontalStackLayout>
            </FlexLayout>
            <Label Text="{Binding ErrorText}" TextColor="{AppThemeBinding Light=#DB5151, Dark=#DB5151}" HorizontalOptions="Center" FontAttributes="Italic"/>
        </VerticalStackLayout>
    </Border>
    
</toolkit:Popup>
