<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:tb="https://notifyicon.com/"
             xmlns:local="clr-namespace:SlippiTV.Client"
             xmlns:system="clr-namespace:System;assembly=System.Runtime"
             xmlns:vm="clr-namespace:SlippiTV.Client.ViewModels"
             xmlns:converters="clr-namespace:SlippiTV.Client.Converters"
             xmlns:mi="http://www.aathifmahir.com/dotnet/2022/maui/icons"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:service="clr-namespace:SlippiTV.Shared.Types;assembly=SlippiTV.Shared"
             x:Class="SlippiTV.Client.FriendsPage"
             x:DataType="{x:Type vm:FriendsViewModel}">
    <ContentPage.Resources>
        <ResourceDictionary>
            <mi:MauiIcon Icon="{mi:FluentFilled Icon=AddCircle24Filled}" IconColor="#10FA94" x:Key="AddIcon"/>
            <toolkit:EnumToBoolConverter x:Key="LiveStatusToVisibilityConverter">
                <toolkit:EnumToBoolConverter.TrueValues>
                    <service:LiveStatus>Active</service:LiveStatus>
                </toolkit:EnumToBoolConverter.TrueValues>
            </toolkit:EnumToBoolConverter>
            <toolkit:EnumToBoolConverter x:Key="ActiveOrIdleToBoolConverter">
                <toolkit:EnumToBoolConverter.TrueValues>
                    <service:LiveStatus>Active</service:LiveStatus>
                    <service:LiveStatus>Idle</service:LiveStatus>
                </toolkit:EnumToBoolConverter.TrueValues>
            </toolkit:EnumToBoolConverter>
            <toolkit:IsStringNotNullOrEmptyConverter x:Key="StringToVisibilityConverter"/>
            <toolkit:IsNotNullConverter x:Key="IsNotNullConverter"/>
            <converters:LiveStatusToColorConverter x:Key="LiveStatusToColorConverter"/>
            <converters:LiveStatusToToolTipConverter x:Key="LiveStatusToToolTipConverter"/>
            <converters:StageToImageConverter x:Key="StageToImageConverter" />
            <converters:CharacterToImageSourceConverter x:Key="PlayerCharacterToImageSourceConverter"/>
            <converters:CharacterToImageSourceConverter IsOpponent="True" x:Key="OpponentCharacterToImageSourceConverter"/>
        </ResourceDictionary>
    </ContentPage.Resources>

    <Grid Padding="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Button x:Name="AddFriendsButton" HorizontalOptions="Start" HeightRequest="48" 
                FontSize="18" Text="Add Friends by Connect Code" Grid.Row="0" FontAttributes="Bold" TextColor="{StaticResource PrimaryText}" CornerRadius="12"
                Margin="0,12,0,24" Clicked="AddFriendsButton_Clicked" BackgroundColor="{StaticResource Tertiary}">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroupList>
                    <VisualStateGroup Name="CommonStates">
                        <VisualState Name="Normal">
                            <VisualState.Setters>
                                <Setter Property="Opacity" Value="1"/>
                            </VisualState.Setters>
                        </VisualState>
                        <VisualState Name="PointerOver">
                            <VisualState.Setters>
                                <Setter Property="Opacity" Value=".85"/>
                                <Setter Property="Scale" Value=".98"/>
                            </VisualState.Setters>
                        </VisualState>
                    </VisualStateGroup>
                </VisualStateGroupList>
            </VisualStateManager.VisualStateGroups>
            <Button.ImageSource>
                <FontImageSource Glyph="{mi:FluentFilled Icon=AddCircle24Filled, IconColor={StaticResource StatusGreen}}" Size="32" />
            </Button.ImageSource>
        </Button>
        
        <CollectionView ItemsSource="{Binding Friends}" Grid.Row="1">
        <CollectionView.ItemTemplate>
            <DataTemplate x:DataType="{x:Type vm:FriendViewModel}">
                <VerticalStackLayout Spacing="4" Padding="0">
                    <BoxView HeightRequest="1"/>
                    <Grid Padding="0,0,0,3">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                            
                        <HorizontalStackLayout Grid.Column="0" HorizontalOptions="Start">
                            <ImageButton Clicked="WatchFriendButton_Clicked" IsEnabled="{Binding LiveStatus, Converter={StaticResource LiveStatusToVisibilityConverter}}"
                                        VerticalOptions="Center" Margin="0" Padding="-4, 6, 0, 0" BackgroundColor="Transparent" >
                                <ImageButton.Source>
                                    <FontImageSource Glyph="{mi:FluentFilled Icon=Tv24Filled, IconColor=#FAFAFA}" />
                                </ImageButton.Source>
                            </ImageButton>
                               
                            <HorizontalStackLayout HorizontalOptions="End" Margin="-8,0,0,0" IsEnabled="True" IsVisible="{Binding LiveStatus, Converter={StaticResource LiveStatusToVisibilityConverter}}">
                                <Image MaximumHeightRequest="14" VerticalOptions="Center" Margin="0,4,0,0" ToolTipProperties.Text="Active Viewers">
                                    <Image.Source>
                                        <FontImageSource Glyph="{mi:FluentFilled Icon=Person24Filled, IconColor={StaticResource StatusGreen}}"/>
                                    </Image.Source>
                                </Image>

                                <Label FontSize="10" VerticalTextAlignment="Center" Padding="2,4,5,2" Grid.ColumnSpan="1" Margin="0" HorizontalOptions="Start">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="{Binding ViewerCount}" TextColor="{StaticResource StatusGreen}"/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </HorizontalStackLayout>
                        </HorizontalStackLayout>

                        <HorizontalStackLayout Grid.Column="1" HorizontalOptions="Start">
                            <Label Text="{Binding ConnectCode}" FontAttributes="Bold" FontSize="24" VerticalTextAlignment="Center"/>
                            <Image MaximumHeightRequest="12" VerticalOptions="Center" Margin="4,2,0,0" ToolTipProperties.Text="{Binding LiveStatus, Converter={StaticResource LiveStatusToToolTipConverter}}"
                                    local:CursorProperties.Cursor="Help">
                                <Image.Source>
                                    <FontImageSource Glyph="{mi:FluentFilled Icon=Circle24Filled}" Color="{Binding LiveStatus, Converter={StaticResource LiveStatusToColorConverter}}"/>
                                </Image.Source>
                            </Image>
                        </HorizontalStackLayout>

                        <Grid ColumnSpacing="6"
                                Grid.Column="2" 
                                HorizontalOptions="Start"
                                VerticalOptions="Center" 
                                Padding="2,0,12,0"
                                Margin="16,0,8,0"
                                IsVisible="{Binding ActiveGameInfo, Converter={StaticResource IsNotNullConverter}}">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" BackgroundColor="Transparent" Padding="6" StrokeShape="RoundRectangle 8" MinimumWidthRequest="84" HeightRequest="56">
                                <VerticalStackLayout  HorizontalOptions="Center" VerticalOptions="Center">
                                    <Button x:Name="PlayerLinkButton" 
                                            IsVisible="{Binding ActiveGameInfo.PlayerDisplayName, Converter={StaticResource StringToVisibilityConverter}}"
                                            Clicked="OpenPlayerWebpage"
                                            VerticalOptions="Center"
                                            HorizontalOptions="Center"
                                            FontSize="14"
                                            TextColor="{StaticResource White}"
                                            BackgroundColor="Transparent" 
                                            Text="{Binding ActiveGameInfo.PlayerDisplayName}"
                                            ToolTipProperties.Text="{Binding ActiveGameInfo.PlayerConnectCode}"
                                            Margin="0,0,0,2"
                                            Padding="0">
                                    </Button>
                                    <CollectionView ItemsSource="{Binding PlayerStocksLeft}" ItemsLayout="HorizontalList" VerticalOptions="Start" Margin="0,2,0,0" MinimumHeightRequest="0" HeightRequest="18">
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate x:DataType="{x:Type system:Int32}">
                                                <HorizontalStackLayout VerticalOptions="Start" Padding="0" Margin="0">
                                                    <Image Source="{Binding Source={RelativeSource AncestorType={x:Type CollectionView}}, Path=BindingContext.ActiveGameInfo, Converter={StaticResource PlayerCharacterToImageSourceConverter}}" WidthRequest="16" Margin="2,0,0,0" VerticalOptions="Start"/>
                                                </HorizontalStackLayout>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </VerticalStackLayout>
                            </Border>

                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" ToolTipProperties.Text="{Binding ActiveGameInfo.Stage}">
                                <Label IsVisible="True" FontSize="12" VerticalTextAlignment="Start" Margin="0" HorizontalOptions="Center" Padding="0">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="Game " />
                                            <Span Text="{Binding ActiveGameInfo.GameNumber}" />
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                                <Label IsVisible="True" FontSize="14" VerticalTextAlignment="Center" Margin="0,-4,0,0" HorizontalTextAlignment="Start" Padding="0" HorizontalOptions="Center" Grid.Column="1">
                                    <Label.FormattedText>
                                        <FormattedString>
                                            <Span Text="vs."/>
                                        </FormattedString>
                                    </Label.FormattedText>
                                </Label>
                            </VerticalStackLayout>

                            <Border Grid.Column="2" BackgroundColor="Transparent" Padding="6" StrokeShape="RoundRectangle 8" MinimumWidthRequest="84" HeightRequest="56">
                                <VerticalStackLayout  HorizontalOptions="Center" VerticalOptions="Center">
                                    <Button x:Name="OpponentLinkButton" 
                                            Clicked="OpenOpponentWebpage"
                                            IsVisible="{Binding ActiveGameInfo.PlayerDisplayName, Converter={StaticResource StringToVisibilityConverter}}"
                                            VerticalOptions="Center"
                                            FontSize="14"
                                            TextColor="{StaticResource White}"
                                            BackgroundColor="Transparent"
                                            Text="{Binding ActiveGameInfo.OpponentDisplayName}" 
                                            ToolTipProperties.Text="{Binding ActiveGameInfo.OpponentConnectCode}"
                                            Margin="0,0,0,2"
                                            Padding="0">
                                    </Button>
                                    <CollectionView ItemsSource="{Binding OpponentStocksLeft}" ItemsLayout="HorizontalList" VerticalOptions="Start" Margin="0,2,0,0" MinimumHeightRequest="0" HeightRequest="18">
                                        <CollectionView.ItemTemplate>
                                            <DataTemplate x:DataType="{x:Type system:Int32}">
                                                <HorizontalStackLayout VerticalOptions="Start" Padding="0" Margin="0">
                                                    <Image Source="{Binding Source={RelativeSource AncestorType={x:Type CollectionView}}, Path=BindingContext.ActiveGameInfo, Converter={StaticResource OpponentCharacterToImageSourceConverter}}" WidthRequest="16" Margin="2,0,0,0" VerticalOptions="Start"/>
                                                </HorizontalStackLayout>
                                            </DataTemplate>
                                        </CollectionView.ItemTemplate>
                                    </CollectionView>
                                </VerticalStackLayout>
                            </Border>
                            <!--<Image Grid.Column="4" 
                                    Source="{Binding ActiveGameInfo.Stage, Converter={StaticResource StageToImageConverter}}" 
                                    HeightRequest="28" Aspect="AspectFit" 
                                    BackgroundColor="Transparent" 
                                    VerticalOptions="Center" 
                                    HorizontalOptions="Start" 
                                    Margin="0,0,0,0"
                                    ToolTipProperties.Text="{Binding ActiveGameInfo.Stage}"/>-->
                        </Grid>

                            <HorizontalStackLayout Grid.Column="3">
                                <ImageButton VerticalOptions="Center" ToolTipProperties.Text="Alerts" Margin="-16,0,-12,0" Padding="0">
                                    <ImageButton.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="NotificationBell_Tapped" NumberOfTapsRequired="1"/>
                                    </ImageButton.GestureRecognizers>
                                    <VisualStateManager.VisualStateGroups>
                                        <VisualStateGroupList>
                                            <VisualStateGroup Name="CommonStates">
                                                <VisualState Name="Normal">
                                                    <VisualState.Setters>
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </VisualState.Setters>
                                                </VisualState>
                                                <VisualState Name="PointerOver">
                                                    <VisualState.Setters>
                                                        <Setter Property="Opacity" Value=".85"/>
                                                        <Setter Property="Scale" Value=".98"/>
                                                    </VisualState.Setters>
                                                </VisualState>
                                            </VisualStateGroup>
                                        </VisualStateGroupList>
                                    </VisualStateManager.VisualStateGroups>
                                    <ImageButton.Source>
                                        <FontImageSource Glyph="{mi:FluentFilled Icon=Alert28Filled, IconColor={StaticResource StatusYellow}}"/>
                                    </ImageButton.Source>
                                </ImageButton>
                                <ImageButton x:Name="FriendsMore" VerticalOptions="Center" Margin="0" Padding="0,8,0,0" Grid.Column="4" HorizontalOptions="End">
                                    <ImageButton.GestureRecognizers>
                                        <TapGestureRecognizer Tapped="FriendsMore_Tapped" NumberOfTapsRequired="1"/>
                                    </ImageButton.GestureRecognizers>
                                    <ImageButton.Source>
                                        <FontImageSource Glyph="{mi:FluentFilled Icon=MoreHorizontal24Filled, IconColor={StaticResource PrimaryLight}}"/>
                                    </ImageButton.Source>
                                </ImageButton>
                            </HorizontalStackLayout>
                        </Grid>
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <local:BottomBar Grid.Row="2" BindingContext="{Binding ShellViewModel}" HeightRequest="20" VerticalOptions="End"/>

        <tb:TaskbarIcon
        IconSource="slippitv.ico"
        LeftClickCommand="{Binding ShowWindowCommand}"
        NoLeftClickDelay="True"
        x:Name="TaskbarIcon">
            <FlyoutBase.ContextFlyout>
                <MenuFlyout>
                    <MenuFlyoutItem Command="{Binding ShowWindowCommand}" Text="Show Window" />
                    <MenuFlyoutSeparator />
                    <MenuFlyoutItem Command="{Binding ExitApplicationCommand}" Text="Quit" />
                </MenuFlyout>
            </FlyoutBase.ContextFlyout>
        </tb:TaskbarIcon>
    </Grid>
</ContentPage>
